[project]
name = "agentic-research-harness"
version = "1.5.0"
description = "Filesystem-first research workflow for AI agents"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
research-harness = "research_harness.cli:main"
